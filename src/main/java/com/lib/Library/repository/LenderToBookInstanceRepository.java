package com.lib.Library.repository;

import com.lib.Library.entity.LenderToBookInstance;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LenderToBookInstanceRepository extends JpaRepository<LenderToBookInstance, String> {
}
